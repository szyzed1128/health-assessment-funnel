# 本批次待解决事项

## 依据与范围

本清单以腾讯文档《【睿迄科技】全栈开发 2 天挑战》为唯一 PRD 依据。每项均标记为：

- `PRD 必做`：PRD 明确要求，未完成前不能验收。
- `工程一致性`：PRD 评分项或端到端闭环所需的实现约束。
- `用户验收要求`：项目负责人已明确提出的额外要求，不将其伪装为 PRD 原文。

本批次不加入 PRD 未要求的行动计划生成能力；健康评估的必填输出仍是 BMI、建议摄入量和目标预测日期。

## 待解决清单

| ID | 类型 | 问题与 PRD 依据 | 本批次处理 | 验收标准 | 状态 |
| --- | --- | --- | --- | --- | --- |
| P0-01 | 工程一致性 | PRD 要求数据持久化的“状态一致性”，评分标准要求录入到支付完整闭环。已评估会话仍可直接调用答案接口写入，可能让已存结果与答案不一致。PRD 未要求用户在结果页编辑答案。 | 答案变更与旧结果作废在同一数据库事务内完成，状态恢复为待评估；普通用户重新测评仍创建新的随机 Session。验收测试也使用独立随机 Session，不影响其他访问者。 | 对 `ASSESSED` Session 的答案写入后，答案更新、旧结果删除、会话状态恢复为待评估；重新计算后只保留一条最新结果；测试覆盖该路径。 | 已完成 |
| P0-02 | PRD 必做 | 第三阶段要求：非会员隐藏具体预测曲线数据，会员 API 返回完整数据；第四部分要求结果页可演示完整 funnel。当前会员页只显示预测周期，没有展示预测点。 | 在会员结果页展示服务端返回的预测点数据；非会员继续完全不可见。 | 免费接口和页面均无预测点；支付后会员页展示预测趋势及目标日期、热量建议。 | 已完成 |
| P0-03 | 用户验收要求 | 项目负责人要求所有前端可见文本均为中文。页面会直接呈现服务端英文错误信息。 | API 保留稳定错误码；前端按错误码映射中文提示，不直接暴露服务端英文消息。 | 主流程、校验失败、网络/服务端异常提示均为中文；自动化测试覆盖至少一个错误映射。 | 已完成 |
| P0-04 | 用户验收要求（PRD 加分项） | PRD 第三大点要求提供一键运行测试方式，CI 是加分项；项目负责人明确希望拿到该加分，因此本项目按必须完成处理。此前 `npm test` 未运行 Playwright。 | 让一个项目命令运行单元、集成和端到端测试；CI 同步运行同一质量命令。 | `npm test` 在干净环境完成数据库准备、Vitest 与 Playwright；GitHub Actions 配置运行该命令；推送 GitHub 后补充真实通过状态。 | 本地已完成；真实 CI 通过状态待 GitHub 仓库 |
| P0-05 | PRD 必做 | PRD 交付物要求：公网完整演示链接、GitHub 仓库链接、`/pay` 可重放调用、已支付测试 Session、Schema 图、AI 使用复盘。 | 补齐 README 和交付证据；部署与推送需要实际外部账号/仓库配置后执行。 | README 可独立指导启动、测试和支付回放，并含全部交付材料链接或明确待外部发布项。 | 文档已完成；公网发布待外部账号 |
| P0-06 | 工程一致性 | 此前 `PROJECT_PROGRESS.md` 与 `PROJECT_PLAN.md` 中保留了早期“阶段 1 待评审、阶段 2/3 未开始”等内容，和当前实现及测试结果冲突，容易误导验收。 | 更新项目进度和计划状态，明确当前代码已完成阶段 1-4，本地测试已通过，外部交付项仍待处理。 | 文档状态与 README、测试结果和当前代码保持一致；不把未发布的公网/CI 说成已完成。 | 已完成 |
| P0-07 | 用户验收要求 | 前端体验反馈：预测趋势不应永远呈直线；BMI 展示需要区间条；测评应收集用户目标日期；数值边界提示与单位位置需要更直观；数字题输入值不能串到下一题。 | 新增目标日期题与 `DATE` 答案类型；结果持久化 `requestedTargetDate`；会员预测趋势改为按目标日期生成非线性曲线；结果页新增 BMI 黄绿红区间条；数字题显示范围提示并把单位放入输入框右侧；表单按题目 key 重建避免旧值带入。 | 前端全中文；免费 API 不返回会员专属目标日期/曲线；会员页显示用户目标日期、系统预计日期和非线性预测趋势；`npm test` 通过。 | 已完成 |
| P0-08 | 用户验收要求 | 新增产品规则：BMI 需要在目标体重前预览；保持体重需要基于 BMI 给出默认目标并跳过目标体重题；减重需要给出建议区间；日期需要支持重要日期与系统预计日期二选一，且默认值不能早于当前日期后 14 天。 | 新增服务端 BMI 预览接口；正常 BMI 的保持体重答案在同一事务中自动写入当前体重并跳过目标体重题；前端依据真实日历动态限制月份和日期，并在提交前按最小/最大日期校正；新增重要日期类型、中文年月日输入和日期来源选择。 | 预览不创建正式结果；正常 BMI 的保持体重不再显示目标体重题；无重要日期时跳过日期输入；2 月 31 日、9 月 31 日等非法日期无法持久化；选择重要日期时后端要求合法且不早于当前日期后 14 天；会员结果显示日期来源和参考日期；免费响应不返回这些字段。 | 已完成 |
| P0-09 | 用户验收要求 | 保持体重在 BMI 偏低或偏高时不能强迫用户输入与当前体重相等的目标；直接减重在 BMI 偏低时也不合理。目标分流必须由服务端保存和校验，不能只靠前端跳转。 | 新增 `goal`、`effectiveGoal`、`goalResolution` 三层状态；保持体重 + BMI 偏低重定向增重，保持体重 + BMI 正常自动保持，保持体重 + BMI 偏高/较高重定向减重；直接低 BMI 减重被阻止；重定向提示页需确认后再进入目标体重题。 | 中国 BMI 分界为 18.5、24、28；重定向提示页刷新后可恢复；目标体重保存时按实际目标校验，减重必须落在健康减重区间，增重在有建议区间时必须落在区间内；单元、集成和 E2E 测试覆盖该逻辑。 | 已完成 |
| P0-10 | 用户验收要求 / PRD 必做 | 第三阶段要求结果页接口校验 `subscription_status`，非会员 API 只返回脱敏数据，`/pay` 修改数据库会员状态。旧前端点击“解锁”会直接调用支付，验收时不容易区分非会员结果、模拟支付页和会员结果。 | 新增 `/result?sessionId=...` 和 `/checkout?sessionId=...`；结果页只读取后端结果接口；支付页要求输入模拟支付码，正确后调用 `/api/pay`，由后端写入订阅状态，再回到结果页重新读取会员结果。 | 同一台电脑可用同一个 Session URL 先验收 FREE 响应和页面，再进入 checkout 输入错误支付码确认不激活，输入正确支付码后回到 result 验收 MEMBER 响应和页面；错误支付码不写 `PaymentEvent` 和 `Subscription`。 | 已完成 |

## 执行原则

1. 不以 mock、前端权限判断、跳过测试或降低数据库/服务端能力的方式完成任何事项。
2. 每个事项完成后运行与其风险相匹配的自动化测试；本批次结束前运行完整 `npm test`。
3. 公网部署和 GitHub 通过状态依赖项目的外部账号与仓库。代码、CI 配置和 README 先完成；没有真实发布前不得声称已上线或 CI 已通过。
4. CI 虽然是 PRD 加分项，但按本项目用户验收目标必须完成；表述时需区分 PRD 原文和用户要求。
