# 健康测评系统：项目范围与验收要求

## 1. 项目定义

本项目需要在两天内交付一个可公网访问、可从头演示到模拟付费完成的健康测评系统。

系统面向希望改善体重与健康状态的用户。用户完成多步骤测评后，服务端基于其身体数据计算健康评估结果；免费用户只能看到经过脱敏的摘要，完成模拟订阅后可查看完整报告。

本项目的核心考核对象是后端工程质量。最高优先级为：服务端健康评估逻辑、订阅鉴权与模拟支付闭环、自动化测试与 CI。数据建模、接口设计和状态一致性必须为上述能力提供可靠支撑。前端需要具备完整且可信的用户流程，但不要求像素级复刻竞品。

## 2. 竞品参考结论

参考产品 BetterMe Pilates 是一个配置化、多步骤的健康测评 Funnel。其主要模式为：

1. 以单题或多选题逐步采集用户目标、身体状况、运动习惯和生活习惯。
2. 在若干关键问题之间插入个性化激励页，依据用户刚刚提交的答案展示不同文案或内容。
3. 所有步骤均可恢复，最终生成个性化健康/训练方案。
4. 通过免费摘要与订阅后完整内容的差异，实现付费转化。

本项目应借鉴这一数据流和体验节奏，但不需要复刻其约 48 个页面、全部题目、营销文案或支付系统。

## 3. 本次必须实现的用户流程

### 3.1 匿名会话创建与恢复

1. 用户首次进入时，前端创建或获取匿名 `sessionId`。
2. 用户可在未登录状态下完成测评。
3. 用户中途刷新、关闭后再次进入，系统可通过 `sessionId` 恢复已填写答案和当前进度。

### 3.2 多步骤测评与增量保存

至少覆盖以下字段：

- 性别
- 健康/体重目标
- 年龄
- 身高
- 当前体重
- 目标体重
- 运动频率

要求：

1. 每完成一个步骤，前端调用后端接口进行增量保存，而不是仅在最终提交时一次性保存。
2. 重复提交同一题时，结果应更新而不是产生重复答案。
3. 服务端必须验证字段类型、必填条件和业务范围。
4. 服务端必须能处理乱序提交，并明确何时允许计算最终结果。

### 3.3 服务端健康评估

用户完成必要字段后，后端必须计算并保存：

- BMI
- 建议每日摄入量
- 目标预测日期

计算逻辑必须完全在服务端执行。前端只展示服务端返回的结果，不自行作为结果可信来源。

### 3.4 免费与会员结果权限

结果接口必须依据订阅状态返回不同内容：

- 非会员：可查看 BMI、概览或有限建议；预测曲线、完整热量计划、详细结果等受保护字段不得返回。
- 有效会员：可查看完整评估数据和受保护字段。

这不是纯前端隐藏。非会员通过直接请求 API 也不得获得受保护数据。

### 3.5 模拟支付闭环

系统必须提供模拟支付接口：

1. 调用 `pay` 接口后，系统将对应会话的订阅状态更新为有效。
2. 支付操作应具备幂等性；同一个支付事件重复回调不得造成异常或重复创建权益。
3. 支付成功后重新获取结果，必须从免费摘要切换为完整结果。
4. README 必须提供可重放的 `curl` 示例。

## 3.6 PRD 第二、三部分的阶段性目标

PRD 第二部分“任务目标”规定系统最终应具备的五项能力；第三部分“核心流程”规定其应按四个后端阶段实现。两者在本项目中映射如下，并作为阶段完成的硬性判断依据。

| PRD 任务目标 | PRD 核心流程阶段 | 本项目阶段性目标 | 完成定义 |
| --- | --- | --- | --- |
| 数据持久化逻辑 | 第一阶段：测评数据流与状态恢复 | 匿名会话、逐步答案、恢复进度均稳定持久化 | 刷新/重新进入后可恢复；重复、乱序、并发提交行为已定义并经过测试 |
| API 设计规范、数据库建模稳定性 | 第一阶段的基础支撑 | 形成类型安全、可扩展的 API 契约和 Prisma 数据模型 | API 方法/路径/错误结构一致；实体关系、唯一约束、索引和迁移可审阅 |
| 服务端计算逻辑 | 第二阶段：服务端健康评估 | 服务端基于完整用户数据计算 BMI、每日建议摄入量、目标预测日期并保存 | 非法/不完整数据不可计算；结果不由前端传入；算法与边界测试通过 |
| 订阅鉴权与权限保护 | 第三阶段：订阅鉴权与模拟支付 | 免费摘要、会员完整报告及幂等模拟支付形成闭环 | 非会员 API 无受保护字段；支付后升级；重复回调安全；相关测试通过 |
| 测试与质量保障 | 第四阶段：测试与质量保障 | 自动化验证覆盖核心与异常路径，且持续由 CI 执行 | `npm test` 一键通过；GitHub Actions 在 push/PR 自动通过；README 说明覆盖范围 |

实施时，只有达到上表对应的“完成定义”，才能将阶段标记为已完成；前端页面完成不可以替代任一后端阶段的验收。

## 4. 推荐的领域模型

最终具体表名可调整，但必须清晰表达以下职责：

| 实体 | 职责 |
| --- | --- |
| `AssessmentSession` 或 `User` | 匿名用户身份、会话状态、当前进度 |
| `AssessmentAnswer` | 每题答案、更新时间、去重/覆盖规则 |
| `HealthAssessmentResult` | 服务端计算出的、可追溯的评估结果 |
| `Subscription` | 会员状态、生效时间、到期时间 |
| `PaymentEvent` | 模拟支付回调记录、外部事件 ID、幂等控制 |

题目定义可以先固定在代码中；用户填写的数据、计算结果和支付状态必须持久化到数据库。不得把整个问卷答案只存成无法验证、无法扩展的前端临时状态。

### 可扩展答案模型约束

核心画像字段（例如年龄、身高、当前体重、目标体重）可作为评估计算的稳定输入，但问卷答案不得为每一道题创建一列。答案模型必须具备以下能力：

1. 用稳定的题目键标识答案，例如 `goal`、`exerciseFrequency`、`gender`；题目显示文案可调整而不影响历史数据。
2. 支持单选、多选和数值三类答案，并在数据库层和服务端校验其存储形式。
3. 同一会话同一题目只有一条当前答案，重复提交执行覆盖更新。
4. 新增非核心题目时，原则上仅新增题目定义、校验规则和前端步骤，不需要为主表新增数据库列或破坏既有 API。
5. 评估算法只能读取其声明依赖的核心答案，不得因新增可选题而改变已有计算的可重复性。
6. 历史答案、评估结果和支付事件必须保留可追溯的创建/更新时间；评估结果记录算法版本。

该设计同时满足轻量化与可扩展性：当前只实现最小必填题集，未来可新增竞品中的睡眠、饮水、柔韧性、饮食习惯等题目，而不重构核心数据关系。

## 5. 技术与工程硬性条件

### 5.1 技术栈

- 前端：Next.js App Router 或其他熟悉的框架。
- 后端：TypeScript；可采用 Next.js Route Handlers、NestJS 或 Express。
- 数据库：PostgreSQL。
- ORM：Prisma。
- 测试：主流 TypeScript 测试方案，例如 Vitest/Jest；端到端可使用 Playwright。

### 5.2 API 设计

API 应使用清晰、资源导向的路径与 HTTP 方法，并保持一致的成功与错误响应结构。至少需要覆盖：

- 创建/获取会话
- 保存单步答案
- 获取恢复进度
- 触发或获取健康评估
- 获取受权限保护的结果
- 模拟支付回调

所有外部输入均需在服务端校验。不得信任客户端计算值、会员状态或进度状态。

### 5.3 前端最低要求

- 可完成完整的多步测评、恢复、结果展示和模拟付费解锁。
- 移动端体验优先，页面节奏应避免把所有字段堆在单一长表单中。
- 至少提供基础的进度反馈、清晰的错误提示和加载状态。
- 不要求像素级复刻 BetterMe，也不要求真实支付。

## 6. 自动化测试与 CI：必须达成

测试是交付基本条件，不是可选加分项。项目必须支持一键运行测试，例如 `npm test`；项目还必须接入 GitHub Actions，在推送和 Pull Request 时自动执行测试。

至少覆盖以下情形：

1. 健康评估算法单元测试：正常计算、非法身高、非法体重、非法年龄、目标体重不合理等边界。
2. 分步保存与恢复集成测试：中断后恢复、重复提交、乱序提交、并发更新。
3. 权限测试：非会员响应不含受保护字段；会员可获得完整字段。
4. 支付闭环测试：调用 `pay` 后订阅状态变化，结果接口随之返回完整结果。
5. 输入校验测试：非法类型、越界数字、缺失字段等请求会被拒绝，并返回一致的错误响应。

GitHub Actions 工作流应使用项目的标准安装方式、数据库准备方式和测试命令。README 必须展示 CI 状态徽章或提供可直接访问的 workflow 链接。

## 7. 部署与交付物：必须达成

最终交付必须包含：

1. 公网可访问的演示 URL，可自行完整体验 Funnel 和模拟支付。
2. 代码仓库及完整 README。
3. README 中的本地启动方式、环境变量说明、数据库迁移/初始化方式、测试命令和测试覆盖说明。
4. README 中可直接重放的模拟支付 `curl` 命令。
5. 一个已完成支付的测试 `sessionId`，用于直接比较会员与非会员返回。
6. 数据库 Schema 图，展示会话/用户、答案/评估记录、订阅、支付事件之间的关系。
7. GitHub Actions CI 配置，并在 README 展示通过状态徽章或 workflow 链接。
8. AI 使用复盘：AI 在建模、Mock 数据、复杂逻辑和测试中提供了什么帮助；至少举例说明一次被否决或修正的 AI 建议及原因。

## 8. 验收标准

以下任一项缺失，项目即不视为完整：

- 公网演示不可用，或无法从测评走到模拟支付与会员解锁。
- 刷新后无法恢复会话进度。
- 健康结果在前端计算，或计算结果未持久化。
- 非会员接口仍返回完整预测/计划等受保护数据。
- 支付后订阅状态未改变，或结果权限未同步改变。
- 缺少核心流程的自动化测试，或测试无法一键运行。
- 未配置 GitHub Actions 自动运行测试，或 CI 未通过。
- 缺少 API/启动/测试/支付调用说明。

## 8.1 PRD 评分矩阵与验收证据

以下矩阵直接提炼自 PRD 第六部分“评分标准”，用于日常实现取舍和最终自检。每一项必须有可复现的代码、测试或文档证据，而非口头说明。

| 评分维度 | 关注点 | 本项目的可验证标准 | 必须提供的证据 |
| --- | --- | --- | --- |
| API 设计 | 路径、HTTP 方法、请求/响应结构是否专业 | 资源导向路径；状态码与错误结构一致；不存在由客户端传入会员状态或计算结果的接口设计 | README API 文档、接口集成测试、真实请求示例 |
| 数据校验 | 能否防止非法数值与越界输入 | 对年龄、身高、当前/目标体重、枚举值、会话 ID 和支付事件 ID 均在服务端校验；拒绝类型错误、范围错误、逻辑矛盾 | 校验 schema、异常路径测试、测试输出 |
| 数据库设计 | 表结构、字段与关系是否可扩展 | 会话、答案、结果、订阅、支付事件职责分离；答案和支付事件有唯一约束；关键查询有索引；结果可追溯 | Prisma schema、迁移文件、Schema 图、设计说明 |
| 权限校验 | 订阅前后是否真的有权限边界 | 非会员 API 响应中不存在完整预测/计划等受保护字段；会员返回完整报告；前端隐藏不作为权限实现 | 结果服务实现、免费/会员响应对照、权限集成测试 |
| 支付闭环 | 模拟支付是否完整可靠 | `pay` 能创建/确认支付事件并激活订阅；重复同一事件不重复发放权益；支付后结果立即升级 | 支付 API、幂等测试、端到端支付前后测试、cURL |
| 状态与异常 | 刷新、中断、重复、乱序和异常是否考虑 | 每步增量保存；恢复返回准确进度；重复/乱序/并发更新行为被明确定义并测试 | 会话/答案服务、集成测试、README 行为说明 |
| 测试与质量 | 是否只覆盖 happy path；能否一键运行 | `npm test` 覆盖算法边界、持久化恢复、权限、支付和非法输入；测试不依赖手工操作 | 测试文件、测试命令、CI 运行记录 |
| CI | 是否自动验证质量 | GitHub Actions 在 push 和 pull request 自动执行测试；主分支 workflow 通过 | workflow 文件、README 徽章/链接、Actions 通过记录 |
| AI 协作效率 | 是否把 AI 用于高质量工程协作且有判断 | 复盘记录 AI 在建模、Mock、逻辑、测试中的用途，及至少一个被否决/修正的建议与原因 | README 的 AI 复盘章节、相关提交/设计记录 |

最终验收时，逐项确认上表“必须提供的证据”均可直接访问。

## 9. 明确不在本次范围内

- BetterMe 页面和题库的 1:1 复刻。
- 真实第三方支付、真实扣款或银行卡信息处理。
- 真实营养/医学诊断；评估算法仅用于演示，应注明其非医疗建议性质。
- 用户注册、密码登录、复杂账号体系。
- 完整运营后台、题库配置后台和 A/B 实验平台。

## 10. 两天实施优先级

第一优先级：服务端健康评估算法及其边界测试；订阅鉴权、受保护字段过滤、模拟支付幂等闭环及其测试。

第二优先级：数据库 Schema、会话与答案持久化、恢复流程、完整且可信的多步前端 Funnel、免费/会员结果页面、部署和 README。

第三优先级：GitHub Actions CI、额外题目、更多竞品式激励页和视觉细节。CI 是必交付项，需在第一、二优先级完成后立即实现；额外题目与视觉细节不得挤占核心测试、CI 与可演示闭环的时间。
